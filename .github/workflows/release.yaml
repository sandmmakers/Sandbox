name: 'Create release'

on:
  push:
    tags:
      - 'v*.*.*'

permissions:
  contents: write

jobs:
  Build:
    name: Build
    uses: ./.github/workflows/build.yaml
    with:
      tag: ${{  github.ref_name }}
  Release:
    name: Release
    runs-on: ubuntu-latest
    needs: Build
    steps:
      - uses: actions/download-artifact@v4
      - name: Create assets
        env:
          tag: ${{ github.ref_name }}
        run: |
          mv Artifacts-Linux Sandbox-${tag}
          tar zcvf Sandbox-${tag}-linux-x86_64.tgz Sandbox-${tag}
      - name: Create release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          tag: ${{ github.ref_name }}
        run: |
          gh release create "$tag" \
              --repo="$GITHUB_REPOSITORY" \
              --title="${GITHUB_REPOSITORY#*/} ${tag#v}" \
              --prerelease \
              --verify-tag \
              Sandbox-${tag}-linux-x86_64.tgz